<h1><%= @catagory.titleize %></h1>

<div class="container">
<div class="row">
  <% @shoots.each do |shoot| %>
   <div class="col-xs-12 col-md-4">
  	<span class="center gallery"><h2><%= shoot.name %></h2></span>
      <%= link_to( image_tag(shoot.thumbnail_url.to_s), image_path( "/shoots/#{shoot.id}/#{shoot.name}/photos/gallery "), class: "thumbnail" )%>
    </a>
</div>
  <% end %>
</div>